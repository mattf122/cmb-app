[build]
  command = "echo 'Build complete'"
  functions = "netlify/functions"
  publish = "."

[functions]
  node_bundler = "esbuild"

[functions."claude"]
  timeout = 26

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache"
